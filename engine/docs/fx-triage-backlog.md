# fx実装トリアージ・バックログ（2026-07-19 全WARN検証の残課題）

出典: lint-fx WARN 86件＋fx発火ゼロ診断を22並列エージェントで公式text/Q&A照合した結果。
適用済み: 検証済みデータfix25件＋エンジン6点（donMinus/deckToTrashのthen実行＝19+1枚の不発解消・lifeAddFromDeck/lifeTrashのoptionalゲート・powerCopy fromAttacker・trashToLife anyCard・fireDonReturnedのonce復元）＋optional等データ追補18箇所。

## 未適用defer（エンジン改修が必要。specは検証済み）

### EB03-031
- **判定根拠**: 公式text「自分のトラッシュにあるコスト7以下のイベント1枚までの、【メイン】効果を発動する」に対し、現行fxは {"op":"draw","n":1}（cards-fx.js:3446）で効果そのものが別物。optional 追加の問題ではなく、トラッシュのイベントの【メイン】効果を発動する新opが必要。Q1095＝イベントの発動とは異なる（「イベントを発動した時」誘発は発火しない）、Q1166＝カードはトラッシュに残る、が実装仕様の制約。
- **修正spec**: engine/src/20-targeting-fx.js の doOp switch（case 'playEventFromHand'＝行1137 付近）に新op case 'trashEventMain' を追加: ①cands = P.trash.filter(c => c.base.type==='EVENT' && (c.base.cost||0) <= (op.maxCost!=null?op.maxCost:99) && c.base.fx && c.base.fx.main) ②const c = P.isCPU ? cands[0] : await chooseCard(side, cands, '【メイン】効果を発動するイベント（トラッシュ・任意）', 'ownBig', true)（「1枚まで」＝常に任意）③選択時 cardReveal → await runFx(c.base.fx.main.fx, {self:c, side})。カードはトラッシュから動かさない（Q1166）・P.trash.push/luffyReveal 等「イベント発動時」フックは呼ばない（Q1095）。④docs/opcg-effect-system-design.md §12 と tools/lint-fx.js の実在op一覧へ登録。実装後 cards-fx.js の EB03-031 の {"op":"draw","n":1} を {"op":"trashEventMain","maxCost":7} に置換。
- **テストspec**: フルフロー回帰: リーダー「サンジ」・トラッシュにコスト7以下イベント（fx.main持ち）とコスト8イベントを置き、EB03-031 を手札から登場（ドン-1コスト込みで playCard 経由）→ ①コスト8が候補に出ない ②発動後イベントがトラッシュに残る（Q1166）③「イベントを発動した時」誘発（OP04-053型）が発火しない（Q1095）④辞退選択で何も起きない、をassert。
- **公式Q&A**: Q1095, Q1166

### OP01-063
- **判定根拠**: optional 以前の問題。公式text「相手の手札1枚を選び、公開する。公開したカードがイベントの場合、相手のライフ1枚までを、持ち主のデッキの下に置く」に対し、現行fxは {"op":"peekOppHand"} のみで条件部（イベント判定＋ライフをデッキ下）が丸ごと未実装。doOp peekOppHand（src/20-targeting-fx.js:601）は手札先頭n枚のログ表示＋op.then の無条件実行のみで公開カードの型チェック機構が無く、相手ライフを持ち主のデッキ下に置くopも存在しない（oppLifeToHand のみ）。Q155/156より手札は裏向き選択かつ相手が並び替え可＝実質ランダムで、選択は rng() で近似可能。エンジン改修必須のため defer。
- **修正spec**: ①src/20-targeting-fx.js case 'peekOppHand' を拡張: op.pick=true 時は相手手札から1枚を rng() でランダム選択（Q155/156: 裏向き・並び替え可＝ランダム等価。ロックステップ不変条件のため Math.random 禁止）し cardReveal で公開、op.thenIfType があれば公開カードの base.type がそれ（'EVENT'）と一致する場合のみ op.then を実行（不一致・手札0枚なら何もしない=Q157）。②新op 'oppLifeToBottom' を追加: 相手のライフ上1枚を reset して持ち主(o)のデッキ最下部へ push、op.optional && !P.isCPU なら confirmUse で辞退可、実行後 fireLifeLeft(o) を呼ぶ（ダメージではないためトリガーは発動しない）。③cards-fx.js OP01-063 を {"act":{"label":"レスト:相手手札公開(イベントならライフ削り)","cost":{"restSelf":true},"fx":[{"op":"peekOppHand","pick":true,"thenIfType":"EVENT","then":[{"op":"oppLifeToBottom","n":1,"optional":true}]}]}} に更新。
- **テストspec**: フルフロー回帰: ①相手手札がイベント1枚のみ→起動（自身レスト）→公開→相手ライフ-1がデッキ最下部へ移動（トラッシュ/手札には行かない）②相手手札がキャラのみ→公開のみでライフ不変 ③相手手札0枚→起動は可能だが何も起きない（Q157）④人間側は optional 辞退でライフ不変 ⑤2エンジン並走で hash 一致（rng選択のロックステップ検証）。
- **公式Q&A**: Q154, Q155, Q156, Q157

### OP05-100
- **判定根拠**: W1のoptional漏れ指摘自体は誤検知: leaveProtect pay:'selfLifeTrash'（20-targeting-fx.js:1330-1335）はconfirmUse（残す/残さない）で任意化済みで「置いてもよい」を満たす（Q529-531の裁定にも抵触なし）。ただしonce:'turn'の消費（p._protTurn=G.turnSeq）がconfirmUseより前の1288-1289行にあり、辞退でも【ターン1回】を消費する＝「辞退は【ターン1回】未消費」の公式運用・プロジェクト規約（engine/CLAUDE.md §3.5/§11-2）に反する。同一ターンに2回離場する端点で挙動差が出るためエンジン改修が必要。
- **修正spec**: src/20-targeting-fx.js の leaveProtect 処理: 1289行の先行消費 `if (prot.once === 'turn') p._protTurn = G.turnSeq;` を削除し、各pay分岐（lifeToHand/leaderPowerMinus/toLifeFaceDown/restOwnCards/restSelf/free/deckBottomOther/selfLifeTrash/targetMinus/restOpp等）の支払い成立後・`return true` 直前に `if (prot.once === 'turn') p._protTurn = G.turnSeq;` を移設する（1288行の既消費チェックは現状維持。pay:'free'（OP10-118）等の無確認分岐は常にreturn trueのため挙動不変）。
- **テストspec**: フルフロー: 同一ターン中にOP05-100エネルを効果で2回離場させるシナリオで、1回目の身代わり確認を辞退→2回目も確認UIが出て身代わりできること（現行は1回目辞退で_protTurn消費済みのため2回目が出ない）。加えて1回目で身代わり実行→2回目は出ない（ターン1回消費）ことalso assert。
- **公式Q&A**: Q529, Q530, Q531

### OP11-073
- **判定根拠**: W1指摘の「自分のリーダー1枚までを…パワー+2000」（leaderBuff）自体は自リーダーへの上昇で辞退に戦略価値がなく近似で実害なし。しかし同効果の発動任意性が未実装: 公式構文【相手のアタック時】【ターン1回】ドン‼-5：…はコスト支払い=発動が任意だが、doOp 'donMinus'（src/20-targeting-fx.js:442）は returnDonChoose（src/10-engine-core.js:123-142）直行で人間に辞退UIが無く、ドンが5枚以上あるとアタックされるたび強制支払いになる。cards-fx.js だけでは直せずエンジン改修が必要。同型の onOppAttack donMinus コスト持ち（OP04-059/060/063/068/069/070/072・OP05-075・OP11-062・OP12-069・EB01-034/037等）にも波及する系統ギャップ。プリセット未収録のため影響はカスタムデッキ限定。Q&Aなし。
- **修正spec**: src/20-targeting-fx.js case 'donMinus'（442行）の冒頭に任意発動ゲートを追加: `if (op.confirm && !P.isCPU && !(await confirmUse(side, 'ドン!!-' + op.n, `ドン‼-${op.n}を支払って効果を発動しますか？`, '支払う', '発動しない'))) { ctx._declined = true; return false; }`。辞退時は ctx._declined=true のみで _committed を立てないため、onOppAttack の once 巻き戻し（src/30-flow-battle.js:487 の `_declined && !_committed` 判定）で【ターン1回】未消費が既存機構のまま成立する。その後 cards-fx.js の OP11-073 onOppAttack の donMinus に "confirm":true を付与（同型カードへの一括付与は別タスクで棚卸し）。CPU は現状どおり常時支払い（n=5 の損得ゲートは別途検討）。
- **テストspec**: フルフロー: CPUが人間側リンリン（ドン5枚以上）にdeclareAttack→確認プロンプト表示・「発動しない」でドン枚数不変かつ同ターン2回目のアタックで再度プロンプトが出る（once未消費）・「支払う」で5ドンがドンデッキへ戻り、コスト宣言→相手デッキ上公開→一致時のみリーダー+2000（ターン中）。

### OP15-091
- **判定根拠**: 公式text「相手のトラッシュのカード1枚までを、持ち主のデッキの下に置く」＝『相手は〜』構文でないため選択者は効果コントローラー、かつ『1枚まで』で0枚可。現行op oppTrashToBottom(src/20-targeting-fx.js:1041)は chooseCard(o,…,false) 固定＝相手が選ぶ・強制で、op.optionalを消費しない。fxにoptionalを足しても無効。既存利用カードOP11-072/091は「相手は自身のトラッシュから…置く」＝相手選択・強制で正しいため、エンジン側に選択者切替と任意化の追加が必要。
- **修正spec**: src/20-targeting-fx.js case 'oppTrashToBottom'(1041-1045行): 選択行 `const t = O.isCPU ? cands[0] : await chooseCard(o, cands, 'デッキ下に置くトラッシュを選択', 'ownSmall', false);` を `const picker = op.chooser === 'self' ? side : o; const t = await chooseCard(picker, cands, 'デッキ下に置く相手トラッシュのカードを選択', op.chooser === 'self' ? 'oppBig' : 'ownSmall', !!op.optional);` に変更（chooseCardは内部でisCPU分岐するためO.isCPU三項は不要になる。既存呼出はchooser/optional未指定→picker=o・optional=false=現行挙動維持）。その後 cards-fx.js の "OP15-091" を {"onPlay":[{"op":"oppTrashToBottom","n":1,"chooser":"self","optional":true}]} に変更。
- **テストspec**: unit: 相手トラッシュ2枚の状態でOP15-091をonPlay発火。(1)人間側が__skipで辞退→トラッシュ不変・デッキ枚数不変 (2)1枚選択→その1枚が相手デッキ最下部へ移動しトラッシュから消える (3)CPU側発動→自動で1枚（強いカード優先）が移動 (4)回帰: OP11-091発火時は従来どおり相手(o)側が選択・辞退不可のまま。

### OP15-119
- **判定根拠**: 公式text「自分のライフの上から1枚までを公開する」＝0枚可の任意。公開はライフ上のカード情報（トリガー有無・コスト）を相手に晒すため辞退に明確な実益がある。現行op revealLifeCostBuff(src/20-targeting-fx.js:524)は確認なしで必ず公開・バフ適用し、op.optionalを消費しない。fxにoptionalを足しても無効→エンジン改修が必要。Q1298(ライフ0は発動不可)は現行の `if(!P.life.length) break` で適合済み、Q1293(表向きでも公開宣言可)も現行適合。
- **修正spec**: src/20-targeting-fx.js case 'revealLifeCostBuff'(524行): `if (!P.life.length) break;` の直後に `if (op.optional && !P.isCPU && !(await confirmUse(side, 'ライフ公開', 'ライフの上から1枚を公開しますか？（公開したカードのコスト1につきパワー+1000）', '公開する', '公開しない'))) break;` を追加（confirmUseはCPU自動true＝CPU挙動不変）。その後 cards-fx.js の "OP15-119" 内 onOppEvent と onOppBlocker の両方の {"op":"revealLifeCostBuff"} に "optional":true を追加。
- **テストspec**: フルフロー: OP15-119を自陣に置き相手が【ブロッカー】発動（declareAttack経由）。(1)人間が「公開しない」→パワー不変・ログなし (2)「公開する」→ライフ上コストN×1000バフがバトル中付与 (3)ライフ0→確認UI自体が出ず発動なし(Q1298) (4)CPU側は従来どおり自動公開でバフ。
- **公式Q&A**: Q1293, Q1298, Q1327

### ST10-013
- **判定根拠**: 「リーダー1枚まで」のleaderBuff自動適用（20-targeting-fx.js:438）は対象が自リーダー1択かつ純有利で実害なし。しかし公式ルール上【登場時】/【アタック時】の『ドン!!-1(…戻すことができる)』コストは支払わない選択が可能（ルールFAQ Q26『コストを支払わないことで効果を発動せずに登場できる』・Q104/Q135同旨）なのに、donMinus（20-targeting-fx.js:442→returnDonChoose 10-engine-core.js:123）は戻し元（アクティブ/レスト）の選択のみで支払い辞退UIが無く強制支払い。アタック毎に人間の意思に反してdonTotal-1される実挙動差があり、donMinus/leaderBuffともoptionalを消費しないためcards-fx.jsの置換だけでは直らない。
- **修正spec**: src/20-targeting-fx.js の case 'donMinus' に op.ask 対応を追加: `if (op.ask && !P.isCPU && !(await confirmUse(side, 'ドン!!-'+op.n, 'ドン!!'+op.n+'枚をドンデッキに戻して効果を発動しますか？', '支払って発動', '発動しない'))) { ctx._declined = true; return false; }` を returnDonChoose 呼び出しの前に挿入（辞退時は後続fx中止・既存の _declined/【ターン1回】未消費パターンと同型）。CPUは従来どおり常に支払い＝既定挙動不変。その後 cards-fx.js の ST10-013 の onPlay/onAttack 両方の {"op":"donMinus","n":1} に "ask":true を付与。同型の donMinus 先頭コスト約180件（例: onPlay donMinus+draw 群）への横展開はbacklogで別途トリアージ。
- **テストspec**: フルフロー回帰: ①人間側で ST10-013 を summon → confirmUse を『発動しない』でスタブ → donTotal 不変・リーダーに buff 無しを assert ②『支払って発動』でスタブ → donTotal-1・リーダー power+1000（untilNextStart で次自ターン開始時に失効）を assert ③CPU側で summon → プロンプト無しで自動支払い＋buff適用を assert（ロックステップ: プロンプトに side 付与を確認）
- **公式Q&A**: Q26, Q104, Q135

### OP10-003
- **判定根拠**: 公式text第2効果「【相手のターン中】【ターン1回】自分がイベントを発動した時、ドン‼デッキからドン‼1枚までを、アクティブで追加する。」に対し、現行fx(cards-fx.js:1198)はonTurnEnd(第1効果)のみで第2効果が丸ごと未実装（lintメモの『onOppAttackにonce無し』は不正確・onOppAttack自体が無い）。エンジンのonSelfEventフック(30-flow-battle.js fireOppEvent内:129-137)はonce:'turn'対応済(OP04-053/OP01-062に実装例)だが、①E.charsのみ走査でリーダー非対応 ②when(oppTurn)判定なし ③fireOppEventの呼び出し元は手札からのメイン/CPU発動3経路(50-input-cpu-ai.js:115/587/679)のみで、カウンターステップの発動経路(30-flow-battle.js:706人間/:842CPU)から呼ばれていない。【相手のターン中】のイベント発動＝カウンターステップのみのため、データ追加だけでは一切発動せずエンジン改修必須。Q795(トリガー効果イベントは対象外)は現設計で準拠、Q794(コスト未払いでも発動)はrunFx後の無条件呼び出しで準拠。
- **修正spec**: engine/src/30-flow-battle.js を3点改修＋cards-fx.jsデータ追加。(a) fireOppEvent内onSelfEventループ(約130-137行): 『const E = G.players[eventSide]; for (const c of E.chars.slice()) { const cfg = c.base.fx && c.base.fx.onSelfEvent; if (!cfg || isNegated(c) || !E.chars.includes(c)) continue;』→『const E = G.players[eventSide]; for (const c of [E.leader, ...E.chars.slice()]) { const cfg = c && c.base.fx && c.base.fx.onSelfEvent; if (!cfg || isNegated(c) || (c !== E.leader && !E.chars.includes(c))) continue; if (cfg.when === "selfTurn" && eventSide !== G.active) continue; if (cfg.when === "oppTurn" && eventSide === G.active) continue;』（既存利用OP04-053/OP01-062はwhen未指定＝挙動不変、リーダーは現在onSelfEvent未使用で安全）。(b) カウンターイベント経路にfireOppEvent追加: 人間側 30-flow-battle.js 約709行『if (c.base.type === "EVENT") await luffyReveal(dSide);』→『if (c.base.type === "EVENT") { await luffyReveal(dSide); await fireOppEvent(dSide); }』、CPU側 約844行『if (x.c.base.type === "EVENT") await luffyReveal(dSide);』も同様。副作用: onOppEvent持ち(OP11-012フランキー/OP11-102ケイミー『相手がイベントを発動した時』)もカウンターイベントで誘発するようになるが公式裁定上正しい挙動変更。それらの回帰テスト要。効果はドン追加等でバトルのパワー計算に非干渉。(c) cards-fx.js: OP10-003エントリに "onSelfEvent":{"when":"oppTurn","once":"turn","fx":[{"op":"donFromDeck","n":1,"mode":"active"}]} を追加（donFromDeck mode:'active'は20-targeting-fx.js:1046-1053の既存op・donMax上限考慮済）。once消費は_selfEventTurn（カードローカル・両エンジン対称）でhash/_HASH_SKIP変更不要。
- **テストspec**: フルフロー回帰: OP10-003リーダー(A側)で (1) 相手ターンのカウンターステップでAがfx.counter持ちイベント(例OP04-016)を発動→Aのドンがアクティブ+1(donFromDeck)されること。(2) 同一相手ターン中に2枚目のカウンターイベントを発動→ドンが増えないこと(【ターン1回】once消費の確認=W2本題)。(3) 次の相手ターンで再度カウンターイベント発動→再びドン+1(turnSeq更新で復活)。(4) 自分のターン中にメインフェイズでイベント発動→発動しないこと(when:oppTurn)。(5) イベントの【トリガー】発動時→ドン追加されないこと(Q795)。(6) 既存第1効果回帰: 自ターン終了時にパワー6000以上《ドンキホーテ海賊団》キャラがいる場合レストのドン1枚がアクティブ化。(7) 副作用回帰: OP11-012/OP11-102がカウンターイベントに反応するようになる挙動の確認。
- **公式Q&A**: Q794, Q795, Q796

### OP12-081
- **判定根拠**: 公式textは2効果構成で、【ターン1回】は第2効果「相手が、元々のコスト8以上のキャラを登場させた時かキャラの効果でキャラを登場させた時、発動できる。相手は自身のライフの上から1枚を、手札に加える」に係る。現行fx（cards-fx.js:939）は第1効果(onLeaderAttack)のみで第2効果が丸ごと未実装＝lint検出の実態は「onceゲート漏れ」ではなく「【ターン1回】付き効果の完全欠落」。実装にはエンジン改修が必須: ①onOppEnterフック(30-flow-battle.js:40)は相手側charsのみ走査でリーダー非対応 ②enteredカードのフィルタ(minBaseCost:8)と「場にあるキャラの効果で登場」(Q996: トリガー/イベント/リーダー効果は発動不可)の判別機構が無い ③「発動できる」=optional対応が無く、Q998(辞退時は後の登場で発動可=once未消費)の消費順序制御も必要。効果op自体は既存のoppLifeToHand(20-targeting-fx.js:560)で表現可能。Q997(両条件同時でも1枚)は単一発火で自然充足。なお第1効果のcond selfCharCount.filter.minBaseCost:8 は公式text「コスト8以上」(「元々の」なし=現在コスト)と厳密には不一致でminCost:8が正の疑いあり(系統バグ型1)—別件として要トリアージ。
- **修正spec**: A) engine/src/30-flow-battle.js summon()内のonOppEnter誘発ブロック(現行40行目)を拡張: (1)走査対象を O.chars.slice() から [O.leader, ...O.chars.slice()].filter(Boolean) にしリーダー対応(存命チェック !O.chars.includes(c) は c!==O.leader の時のみ適用。onceフィールドは c._oppEnterTurn を共用) (2)cfg新キー enteredFilter=登場カードへのmatchFilter(不成立ならスキップ)と orByCharEffect:true=enteredFilter不成立でも「場にあるキャラの効果による登場」なら発動。判別は const src=_fxSrc(); src && src.base.type==='CHAR' && G.players[src.owner] && G.players[src.owner].chars.includes(src)（_fxSrcは20-targeting-fx.js:83・クラシック連結の共有スコープで参照可。トリガー解決中のカードはlimboでcharsに不在のためQ996を自然充足。イベント/リーダー効果発はtype!=='CHAR'で除外） (3)cfg.optional:true対応: 人間はconfirmUseで確認しCPUは自動発動、once消費(c._oppEnterTurn=G.turnSeq)を発動確定後に移動(辞退=未消費でQ998充足)。B) エンジン改修後に cards-fx.js の "OP12-081" へ "onOppEnter":{"once":"turn","optional":true,"enteredFilter":{"minBaseCost":8},"orByCharEffect":true,"fx":[{"op":"oppLifeToHand","n":1}]} を追加（onOppEnterのrunFxはside=コアラ側で呼ばれるためoppLifeToHandの対象=登場させた側で正しい）。C) 副作用分析: 既存のonOppEnter利用はOP04-024シュガー1枚のみ(cfg.cond/when/onceだけ使用)で新キーは全てopt-in・既存挙動不変。Gへ非対称フィールドは追加しないため_HASH_SKIP変更不要。
- **テストspec**: tests/fx-cards.jsにフルフロー回帰を追加: me=コアラL(OP12-081)。(1)相手が手札から元々コスト8のキャラを通常プレイ→発動確認→承諾で相手ライフ-1/相手手札+1を確認。同一ターン内に2体目のコスト8キャラ登場→発動プロンプトが出ない(【ターン1回】消費済み)ことを確認。(2)Q998: 1体目の登場で辞退→同一ターン2体目の登場で発動できる(辞退はonce未消費)。(3)元々コスト7以下を通常プレイ→発動しない/場のキャラの【登場時】効果でコスト7以下を登場→発動する(orByCharEffect)。(4)Q996: 【トリガー】効果でキャラを登場→発動しない。(5)相手ライフ0枚での発動→フリーズせず空振り(oppLifeToHandはlife.length 0でbreak済みだが端点として明示)。
- **公式Q&A**: Q996, Q997, Q998

## fix適用済みカードの残課題（軽微・後日でよい）

- **ST20-002**: （任意の追加改善・backlog）src/20-targeting-fx.js:1288-1289 は once:'turn' の消費（p._protTurn=G.turnSeq）を confirmUse より前に行うため、辞退でも【ターン1回】が消費される（CLAUDE.md §11-2の理想は辞退=未消費）。恒久修正は _protTurn の設定を各payブランチの成功時（return true 直前）へ移す。OP05-100等 once:'turn' 持ち leaveProtect 全カード共通の既存近似。
- **OP13-100**: （任意の後続改善・本fixの前提ではない）辞退時のターン1回未消費(Q372型)を厳密化する場合: src/30-flow-battle.js checkAllyEnter(213-225行)をonLifeLeave(160-165行)と同型に変更 — once消費前にprevSeq退避し、runFxのctx._declined && !ctx._committedならL._allyEnterTurn=prevSeqへ復元。併せてdoOp 'donAttach'のtarget:'chooseOwn'分岐(20-targeting-fx.js:448)でchooseCardがnull(辞退)のときctx._declined=trueを設定(対象を選んだらctx._committed=true)。副作用: onAllyEnterの他利用カード(OP02-026等はonce+強制fxのため_declined不発生で挙動不変)。

## 検証済み・修正不要と判定した設計近似（再調査不要の記録）

- **EB01-050**: 公式text「デッキの上から1枚までを、ライフの上に加える」は任意で、使用op lifeAddFromDeck（src/20-targeting-fx.js:568）は op.optional を消費しない実装。ただし本カードは【カウンター】イベント＝発動自体が任意で、発動を選んだ時点でライフ追加（唯一の効果・純利益）を辞退する合理的場面が存在しない。意図的近似として実害なし。lifeAddFro…
- **EB04-031**: 公式text「ドン‼1枚までをアクティブで追加し、さらに1枚までをレストで追加する」は任意だが、doOp donFromDeck（src/20-targeting-fx.js:1047）は optional を消費せず room=donMax-現在値 で自動追加する設計。①起動メイン自体の起動が任意（プレイヤーが使わない選択可）②追加は上限で自動調整 ③ドン追加は純粋有利でアクティブのみ追加しレス…
- **OP04-060**: 公式text「ドン!!-2：…自分のデッキの上から1枚までを、ライフの上に加える」。lifeAddFromDeck 自体は optional を消費しないが、前段の donMinus（src/20-targeting-fx.js:442）が returnDonChoose で辞退可能＝ドンを払わなければ効果全体が不発になるため、効果の任意性は入口で担保されている。純コストのドン2枚を払った上でライ…
- **OP09-007**: 公式text「自分のパワー4000以下のリーダー1枚までを、このターン中、パワー+1000」＝任意だが、対象は自リーダーのみで、自ターン中のターン終了まで+1000は辞退が得になる局面が公式プールに存在しない純利得。leaderBuff（20-targeting-fx.js:438）は optional を消費しない設計で、自動適用は挙動影響が実質ない意図的近似。条件は leaderEffPowe…
- **OP09-012**: lintの誤検知。「代わりにこのキャラをトラッシュに置いてもよい」の任意性は、leaveProtect pay:'koSelf' 分岐（20-targeting-fx.js:1364-1369）の confirmUse『「モンスター」をKOして…守りますか？』（守る/守らない）で既に実装済み。onlyKO:true・targetFilter nameIncludes ボンク・パンチ・効果KO限定（…
- **OP09-064**: 公式text「ドン‼-1：自分の特徴《キッド海賊団》を持つリーダー1枚までを、アクティブにする」。activateOwnChar の incLeader は optional を消費せず無条件に P.leader.rested=false（20-targeting-fx.js:851）だが、①コストの donMinus 自体が returnDonChoose で辞退可能（:442 ctx._dec…
- **OP09-105**: トリガー「デッキの上から1枚までを、ライフの上に加える。その後、自分の手札2枚を捨てる」。手札2枚捨ては「その後」＝加えたか否かに関わらず強制（discardOwn n:2 で正実装）のため、ライフ追加（純利得）を辞退して得になる局面が存在しない。lifeAddFromDeck の自動実行（20-targeting-fx.js:568・optional非対応）は挙動影響が実質ない近似。Q779（手…
- **OP09-106**: 公式text「自分のリーダーの「ニコ・ロビン」1枚までを、このターン中、パワー+3000」の「1枚まで」は任意だが、対象は自分のリーダー1枚に固定で、+3000を辞退することに戦略的価値が存在しない（自リーダーのパワー上昇を咎める相手効果は無い）。実装は cond leaderName→leaderBuff（cards-fx.js:1581）で、leaderBuff（src/20-targetin…
- **OP13-001**: 公式text「自分のドン‼を任意の枚数レストにできる。レストにしたドン‼1枚につき、このリーダーか…キャラ1枚までを…+2000」。restDonForBuff(src/20-targeting-fx.js:814)の人間パスは1枚ごとにconfirmUse→『やめる』でいつでも打ち切り＝0枚を含む任意枚数を担保しており、任意性は実装済み（誤検知）。レスト確定後の対象選択がoptional=fal…
- **OP14-077**: 公式text「自分のリーダーかキャラ1枚までを…+4000。その後、相手のパワー6000以上のキャラがいる場合、ドン‼デッキからドン‼1枚までを、レストで追加する。」。現行はcounterBuff＝バトルの防御対象(ctx.target)へ自動適用＋donFromDeck自動追加で、どちらも任意化機構を持たない意図的な設計近似。counterBuffの「1枚まで」: バトル中限定のバフを防御対象以…
- **OP14-078**: 公式text「自分のリーダーかキャラ1枚までを、このバトル中、パワー+2000」は任意＋対象選択だが、counterBuff（src/20-targeting-fx.js:441）はctx.target（バトル対象）へ自動適用する設計で、optionalも対象選択も消費しない。これはcards-fx.js内の全カウンターイベント共通のエンジン設計近似。+2000は有利方向で辞退の実益がなく、バトル…
- **OP14-088**: 公式text「相手のコスト1のステージ1枚までを、KOする」は任意だが、koStage（src/20-targeting-fx.js:789）はfilter一致の相手ステージを自動KOする実装でoptionalを消費しない。ステージは1枚しか置けず対象選択の余地はなく、相手ステージのKOは常に有利方向で辞退する実益が実質ない（コスト1ステージにKO時誘発を持つカードは存在しない）ため意図的近似とし…
- **OP15-116**: 「デッキの上から1枚までを、ライフの上に加え」は任意(0枚可)だが、lifeAddFromDeck(src/20-targeting-fx.js:568)は任意化機構を持たない自動追加設計。辞退しても後続の「自分の手札1枚を捨てる」は独立節で回避できず（Q1291で手札0/ライフ0でも追加可＝捨てるはコストでない）、ライフ追加は防御資源増で実質有利方向のみ。辞退に実益がある局面が事実上なく、意図的…
- **OP16-075**: 「ドン!!1枚までをアクティブで追加し、さらに1枚までをレストで追加する」は任意(各0枚可)だが、donFromDeck(src/20-targeting-fx.js:1047)は任意化機構を持たない自動追加設計で、donMax上限は既にクランプ済み。ドン追加を辞退する実益のある局面は事実上存在せず、エネルリーダー効果「3ステップを常に実行」(engine/CLAUDE.md §4)と同型の意図的…
- **P-046**: lintの誤検知。「置いてもよい」の任意性は op handAllToBottomDraw(src/20-targeting-fx.js:186-190) 内部で実装済み: 人間には confirmUse「手札すべてをデッキの下に置き、同じ枚数引きますか？」のY/N確認があり辞退可能。CPUは手札3枚以下のときのみ実行するヒューリスティック。fx側にoptionalキーを足す必要はない（opはop…
- **P-077**: 「ドン‼1枚までをレストで追加」「紫のステージ1枚までをアクティブにする」はともに任意だが、donFromDeck(src/20-targeting-fx.js:1047)・activateStage(同:185)とも任意化機構を持たない自動適用設計。ドン追加も自ステージのアクティブ化（再利用可能化）も有利方向のみで、辞退に実益のある局面が事実上なく意図的近似として実害なし。ステージはfilter…
- **ST04-001**: 公式text「相手のライフ1枚までをトラッシュに置く」の「まで」は任意だが、これは【起動メイン】【ターン1回】ドン!!-7のプレイヤー起点効果（actOnceはtext【ターン1回】由来で有効・10-engine-core.js:319）。7ドン支払い後にトラッシュを辞退する行動は「発動しない」より常に損で合理シナリオが存在せず、挙動影響は実質ゼロ。lifeTrash（20-targeting-f…
- **ST08-013**: 誤検知。「バトルした相手のキャラをKOしてもよい」の任意性は koBattledTarget op内部で実装済み: 人間は confirmUse『KOしますか？（そうした場合このキャラもKO）』で辞退可・CPUはパワー比較ヒューリスティック（20-targeting-fx.js:182）。fx JSONにoptionalキーが無いだけでopが本質的に任意。Q394（バトルでKO済みなら発動不可）も…
- **ST15-003**: 公式text「【相手のターン中】このキャラが効果でKOされた時、自分のリーダー1枚までを、このターン中、パワー+2000」。「1枚まで」は任意だが対象は自リーダーに自動確定。現行fxのleaderBuff（src/20-targeting-fx.js:438）はoptional非対応の自動適用だが、相手ターン中の自リーダー+2000は防御専用の純利益で、辞退が有利になる局面（自リーダーのパワー参照…
- **OP13-057**: 発動経路は健全＝CPU選好による不使用。再現: ライフ1でmain実行→ドン1レスト+P.denyBlock=true（30-flow-battle.js L519でリーダーへのアタック時ブロックステップ自体をスキップ・L325のbeginTurnで失効）、ライフ2ではcond不成立でdenyBlockは立たない。restDonCostは「できる：」どおりconfirmUseで任意(CPUはtru…
- **OP15-076**: main/counterとも発動経路は健全＝CPU選好+サンプリング。main再現: ドン-1+1ドロー+相手キャラ-1000が正しく解決し、CPUフルフロー(heuristicTurn)でも条件成立時に発火(fired main:1)。CPUのmain使用は三重ゲート（eventWorth=相手にpower5000+/ブロッカー/fx持ちの標的が必要 × 両用温存=自ライフ4+ × 非除去両用は…
